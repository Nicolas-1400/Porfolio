<div class="seccion-contacto">
  <div>
    <h1 class="section-title-send">Contacto</h1>

    {{#if exito}}
      <div class="mensaje-exito-enviado">
        ✓ Mensaje enviado. ¡Gracias!
      </div>
    {{/if}}
    <button
      onclick="history.back()"
      class="btn-primary"
      style="margin-top: -150px;"
    >← Volver</button>

  </div>

</div>